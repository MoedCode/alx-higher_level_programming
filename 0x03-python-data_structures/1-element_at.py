     i in range(len(my_list)):
        if(i == idx):
            return (my_list[i])
    return Nt(my_list, idx):
    if (idx > len(my_list)):
        return None

    for i in range(0, len(my_list)):
        if (i == idx):
            return (my_list[i])

    return Noneone
